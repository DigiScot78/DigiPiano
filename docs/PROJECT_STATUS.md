# Project Status

## Purpose
DigiPiano is a browser-based piano learning application. Its original proof-of-concept milestone established the complete technical path from a local MusicXML/MXL score to rendered notation, normalized expected events, live Web MIDI input, correct-note recognition, and score-position advancement.

## What Works
- DigiPiano now opens on a dedicated branded home page using separate light/dark splash artwork derived from the supplied `images/logo_icon_theme.png` brand sheet. A single **Start learning** action enters the existing Learning Home while preserving the full score/practice workspace behind it. The extracted square brand mark is used as the browser favicon, Apple touch icon, and compact mark beside DigiPiano in the application header.
- Platform Phase 1 domain readiness has started without changing the guest experience. `src/platform/domain.ts` defines explicit v1 DTOs for score/version identity, durable musical anchors, saved selections, bookmarks, completed attempt summaries, aggregate progress, immutable course versions, and enrolments. Boundary validators reject unsupported versions, cross-version or reversed ranges, and duplicate course ordering/step identities. Current playback completion flows through the pure `buildAttemptSummary` boundary: one calculation supplies both existing session history and a persistence-ready v1 record. Browser-only repository contracts and in-memory guest adapters now cover score bytes/library metadata, learning records and idempotent attempt aggregation, ordered course progress, account preferences, and guest entitlements. They return defensive copies and introduce no backend SDK or visible workflow change.
- Account-versus-device preference ownership is explicit in `src/platform/preferenceOwnership.ts`. The account-safe v1 preference payload contains the learner's tempo build-up choice. Audio/metronome output, playback controls, piano/Synthesia presentation, piano shortcuts, selected MIDI input, appearance, and workspace geometry remain local to the current device and retain their existing storage keys and UI behaviour.
- Platform Phase 1's deterministic anchor resolver now matches immutable score version plus part, measure, absolute quarter, staff, and optional voice/source-note hints while ignoring disposable cached indexes. Missing, cross-version, or genuinely ambiguous locations return a visible-repair domain result rather than silently selecting nearby music. Attempt repositories also reject unsupported domain/scoring versions. All Phase 1 deliverables are now complete without backend imports in MIDI, playback, audio, or rendering code.
- Hosted Guest Beta is live at `https://digipiano.vercel.app/`. The user has confirmed the production site with real MIDI playing and audio. A whole-app error boundary provides a local-data-safe reload screen, runtime environment names are explicitly limited to local/staging/production, and `vercel.json` defines CSP, MIDI Permissions Policy, anti-framing, referrer, content-type, opener, and immutable asset-cache headers. `npm run verify:deployment` runs the complete typecheck/lint/test/build gate, while `docs/HOSTED_GUEST_BETA.md` records desktop Chrome/Edge support, Web MIDI origin requirements, local-file privacy, environment rules, and staging/production smoke checks. No telemetry vendor has been selected or enabled.
- Ordinary idle score selections now expose the same centred ear audition control used by Guided planning, including toggle-to-stop and muted-audio notice behaviour. The Practice sidebar has a persistent Learning entry across its modes and, when an ordinary score is open, direct Plan whole score and selection-dependent Plan selected passage actions.
- Clear Selection has moved out of Debug and now sits immediately after Reset in both floating and permanent transport toolbars, separated from performance controls. Every Foundations lesson also exposes a reusable ear-button demonstration: it plays and visually presses the lesson notes, while rhythm and phrase lessons add accented four-beat metronome clicks and a smoothly moving notation marker that traverses the full space occupied by two-beat notes.
- Learning Home now includes a Beginner Foundations path for genuinely new pianists. Its first pressure-free lesson teaches the two-black-key landmark and guides fresh MIDI presses through middle C, D, and E with specific retry feedback, no countdown, score, or pass/fail state. A labelled, properly proportioned two-octave piano keeps middle C spatially centred, highlights the requested key, and displays currently held MIDI keys in red; this reusable teaching visual is ready for later fingering lessons. The route finder sends new learners here while Keyboard Check remains an optional setup tool. The staged follow-on curriculum is recorded in `docs/BEGINNER_FOUNDATIONS_PLAN.md`.
- Beginner Foundations now has an in-place lesson selector and a second five-note-position lesson. It maps right-hand fingers 1–5 onto C4–G4, displays the current finger number directly over the target key, and guides a ten-step ascent/descent pattern with compact forward-progress feedback while retaining the path's pressure-free behaviour.
- Beginner Foundations now presents its ten lessons as a session-level course in a compact, unit-aware strip: the active title and Unit 1 position remain clear, ten small numbered navigation targets turn into checks when completed, and a 0–10 progress bar advances while every lesson remains freely accessible. The redundant large course hero has been removed and progression actions dock to the lesson surface so the playable content and Continue/Next controls remain much more visible on a 1080p display. Future lesson groups can reuse the same bounded strip as new units. Completion state deliberately lasts only for the open Learning workspace; account-backed durable course progress remains a later platform milestone.
- A third Foundations lesson links middle C, D, and E to their positions below and on the treble staff. Each prompt pairs a stable-height notation card with the same spatial piano guide, mirrors currently held C3–C5 keys as red labelled markers on the staff, then reinforces recognition through a six-note C–D–E–E–D–C MIDI sequence.
- Foundations Lesson 4 now prototypes duration-aware teaching on middle C: hear/watch demonstrations, guided quarter notes and half-note holds, then an optional two-bar play-through. Guided bars begin on the learner's first fresh C; the pulse continues at 60 BPM through the full bar and final release. MIDI attack/release checks allow 250ms and give specific retry feedback for short/long holds, missed attacks, or extra presses without percentages. After each attempt, every written note carries an on-score On time, Early, Late, or Missed badge so the precise problem location is visible. The compact teaching piano is centred. Other Foundations lessons retain their previous note-on-only guidance pending user feedback on this prototype.
- Foundations Lesson 5 now extends Lesson 4's duration-aware prototype to changing pitches: the complete C–D–E | D–C–E phrase starts on the learner's first C, runs at 60 BPM, waits through the final E release, and reviews every written note as On time, Early, Late, or Missed. It retains hear/watch playback, a moving score marker, pressed-note overlay, and centred compact piano; successful completion advances to Lesson 6.
- Foundations Lesson 6 introduces bass clef and left-hand orientation. It uses bass C (C3) as a new landmark, maps C3–D3–E3 onto a bass-staff card and the compact keyboard, shows left-hand fingers 5–4–3, demonstrates the full up-and-down pattern, and keeps the transition into Sight Reading at the end of the expanded sequence.
- Foundations Lesson 7 gently introduces two-hand coordination by alternating C3–E3 with C4–E4 before a final C3+C4 octave. Both target keys and finger numbers appear together; the last chord accepts either arrival order and asks the learner to hold the first note while adding the second. The selector tiles share a fixed minimum height so later lessons remain visually consistent.
- Foundations Lesson 8 now extends the duration-aware teaching prototype to silence. Its two continuous bars start on the learner's first C and assess three note attacks/releases plus a one-beat and two-beat rest; sounding or holding a key through the body of a rest marks that exact rest as Sound, while successful silence is marked Quiet. Note feedback remains On time/Early/Late/Missed, and the complete result is attached directly to the notation.
- Lesson 8's marker interpolates between the actual unevenly spaced notation symbols, so its visual arrival now matches the MIDI onset expected at every note/rest boundary. Middle C is drawn on its ledger line and D below the staff rather than incorrectly placing C on the E4 line.
- Foundations Lesson 9 introduces sharps and flats as one-key movements from familiar natural notes. A dedicated treble-staff card places the accidental before the note, the compact piano uses the active written spelling on the target black key, and the sequence explicitly demonstrates that C-sharp and D-flat share one physical pitch before adding F-sharp and E-flat.
- Foundations Lesson 10 is now a continuous 60 BPM, duration-aware capstone across all four bars. It checks every right- and left-hand pitch and release, C-sharp, both pitches and releases of the two-note ending, and the final two-beat silence. Compact feedback is attached to each event on the full grand staff before the Sight Reading handoff.
- Vite React TypeScript app starts locally with `npm run dev`.
- `.mxl`, `.musicxml`, and `.xml` files can be selected from the local computer.
- `.mxl` files are decompressed in-browser with `fflate`.
- OpenSheetMusicDisplay renders selected scores as conventional notation and owns responsive system wrapping; imported MusicXML system/page breaks remain diagnostic metadata rather than fixed display constraints.
- MusicXML is normalized into ordered playable `ScoreEvent` objects with per-note written duration, staff, pitch spelling, active clef, arpeggio direction/group metadata, key-signature metadata, and lightweight import diagnostics.
- The app requests Web MIDI access, lists MIDI inputs, and subscribes to the selected input. Every raw message is captured in a bounded ordered event buffer with its own held-before/held-after snapshot, preventing React render batching from dropping rapid chord note-ons. This has been smoke-tested with a real keyboard by the user.
- MIDI note-on, note-off, velocity-zero note-off, and sustain pedal messages are decoded; real keypresses have been observed by the user.
- Held notes are compared with the current expected event; user-confirmed real-keyboard progress works after the renderer lifecycle fix.
- Chords advance only when every expected filtered note is held; extra notes are reported but do not block progress.
- Correct held notes render as green score feedback markers, wrong held notes render as red markers, and note-name text can be toggled independently for correct and wrong feedback. Vertical placement combines OSMD's measured staff lines with the active per-note MusicXML clef, including lower-staff treble, F/C clefs, non-default lines, octave changes, and clef changes. Correct notes use their written staff and wrong notes use the nearest active expected note's staff.
- After a correct event advances, the still-held completed notes are suppressed from wrong-note feedback until released so they do not appear as mistakes against the next event.
- Correct notes from a completed event remain anchored at that event for 450ms, staying solid briefly before fading; wrong-note feedback remains live-only.
- Visual score selection uses a smooth freeform drag/resize outline as a positioning guide while event-aligned clear/faded regions use true anchors and neighbour midpoints to show the exact range that release will commit without exposing adjacent events.
- Selected ranges can play once or loop, and practice can be filtered to both hands, right hand staff 1, or left hand staff 2; one-hand practice skips events with no notes for the selected hand.
- One-hand modes apply a lighter white-wash fade to the inactive staff across the full score or within the active selection preview, while feedback and interaction overlays remain clear.
- Practice hand selection now uses staff-aligned RH/LH toggles beside the score and prevents disabling the final active hand; loop mode lives in an extensible floating toolbar above the score or active selection.
- The score occupies the primary page area beneath a permanently fixed application header. The header keeps DigiPiano at the left, the imported score title plus subtitle/movement or composer in the centre, and compact Open Score and Settings controls at the right. MusicXML work/movement/typed-credit metadata is preferred, with controlled centred-credit inference and a clean filename fallback. The centred Settings modal remembers the chosen MIDI input and reconnects automatically when browser permission is already granted.
- The right sidebar is a persisted right-docked workspace panel with a 240–640px drag-resizable width, a collapse control, and a fixed right-edge restore tab. It reserves score width while its viewport-fixed surface stays between the application header and bottom screen margin, layering above the piano, Synthesia, and timeline in its own column without moving during page scroll. The fixed application header keeps compact Open Score and Settings controls available even when the panel is hidden, while the workspace's keyboard-accessible Practice/Debug tabs give the selected tab the only scrolling body; Debug is no longer trapped in a nested miniature pane. Every fresh Play run docks an initially open panel through countdown, playback, pauses, gates, and loop waiting, then restores it on natural completion or explicit Stop; manual restoration cancels hiding for that run. An optional persisted Play-fullscreen setting uses the browser Fullscreen API for the run, exits only app-owned fullscreen on completion/Stop, and degrades to ordinary playback with a notice if fullscreen is unavailable. Separate persisted 8–300px margins for panel-open and panel-docked layouts symmetrically resize the score paper and trigger OSMD reflow.
- Settings now use a wide responsive four-tab General/Appearance/Piano/Play workspace with keyboard-accessible navigation and grouped cards. They include a persisted System/Light/Dark application theme and independent Paper/Night score styles. Paper uses a warm textured page; Night asks OSMD to render pale notation on a dark page, with all fading and interaction overlays using matching score tokens.
- The current-position marker now separates its solid foreground line from a configurable translucent wash rendered beneath the notation. Appearance settings persist wash colour plus independent note/rest opacity, retain a theme-aware default, and show both states with their leading lines in a live miniature staff preview. Timed playback combines exact note/rest OSMD anchors with meter-aware empty-beat anchors without changing note-only learning or scoring; playback notes reuse idle X positions, while marker height remains fixed to the active system's staff-line span plus 40px above and below. Its right edge retains the original alignment while its left edge extends 6px farther.
- A permanent full-width practice toolbar is fixed to the bottom of the viewport so transport and view controls remain available while long scores scroll. Its left group independently toggles Piano, Synthesia, and Learning, its centred group owns Play/Pause plus a matching attached Play-mode control, Stop, Loop, Clear, and Reset, and Score Audio sits at the right. Both Play-mode buttons open the same document-level modal, isolating its app colours and pointer interaction from score-paper controls; it presents all three mutually exclusive choices as radio cards and its footer owns the persisted Show progress while playing preference. Results are always recorded but otherwise remain hidden until Pause, Stop, or completion. A different mode can be selected at any transport phase; doing so performs the ordinary Reset cleanup before applying it. The optionally visible piano supports 88/76/61/49-key and validated custom ranges, three heights, Auto/Fit/Scroll width modes, optional scientific note labels, and accessible expected/correct/wrong/carried-note states. Existing collapsed-piano preferences migrate to the new piano-visibility setting.
- An idle-only, session-only Learning reference surface replaces the visible score workspace without unloading OSMD. Its accessible Chords and Scales tabs cover 17 common written roots with consistently proportioned C4–C6 piano diagrams. Chords show root-position major, minor, augmented, and diminished triads in the configured RH colour. Scales show major, natural minor, harmonic minor, and ascending melodic minor in the same solid RH colour, with LH numbers stacked above outlined RH numbers directly on every highlighted key. Learning and Synthesia are mutually exclusive while configured piano visibility remains unchanged. All 136 catalog entries replace the current score with deterministic in-memory MusicXML exercises generated from shared templates. Chords remain RH-only; scales contain both staves, printed catalog fingerings, mode-correct descent, and spelling-aware signatures/accidentals, while ordinary score RH/LH controls filter the hands to practise. Catalog fingering and rare-key spelling remain subject to musical review.
- Piano highlight colours are configurable in Settings. Timed and untimed expectations fully fill keys with independent RH/LH Play colours derived from the exact score event/gate rather than pitch matching, while Synthesia has its own configurable RH/LH pair. A persisted, default-off Piano option can display all valid MusicXML fingerings from the loaded score on their corresponding keys, filtered by the main RH/LH mode and stacked above optional note names. Pause-at-each-note previews reveal the complete next gate together at reduced strength one local quarter-note beat before its onset, then promote it to full strength at the gate. Correct held keys remain green for their written duration and become neutral-carried rather than wrong after expiry. Normalized key geometry and panel-level visual tokens are shared by the keyboard and falling-note view without coupling layout to pixels.
- A tempo-aware timed Play mode follows embedded MusicXML tempo changes (with a configurable fallback BPM), supports full-score or selection playback, and drives the score cursor plus duration-aware piano expectations through rests and tempo changes.
- Both transport toolbars now expose a synchronized metronome toggle and attached tempo/count-in options. Tempo defaults to the written score and scales every embedded change from 40–200% without flattening the tempo map; it resets to 100% for each newly opened score and may be changed only while idle. MusicXML meter and measure timing drive accented simple/compound clicks plus persisted Off/1/2-bar musical count-ins. Metronome enablement and volume persist independently from score/piano sound, clicks stop at pauses and note gates, and performance histories are separated by tempo percentage.
- Timed Play can now produce a lightweight piano-like Web Audio voice from the same hand-filtered playback plan used by the score and Synthesia view. Clicking any visible on-screen piano key auditions that pitch through an isolated synth voice, including during playback, and follows the shared mute/volume setting. Both transport toolbars expose synchronized persisted mute/volume popovers. Audio follows score tempo, selections, hand filters, countdowns, stops, and loops; raw MIDI input remains silent.
- Both transport toolbars now share Play/Pause/Resume, Stop, and Reset controls. Manual pause freezes score, Synthesia, audio, and note gates; resume uses the configured countdown before continuing from the frozen position. Stop ends the unfinished attempt, returns to the active full-score or selection start, and preserves recorded markers for review. Reset returns to the same start and also clears those results.
- Starting with either Play button or Space begins from the currently displayed practice event; Reset is the explicit shortcut back to the active plan start. Countdown presentation is fixed above the Synthesia surface, and system following uses the permanent title header plus notation-toolbar clearance as its top offset.
- Timed Play modes have a configurable countdown, Play/Pause/Resume and Reset controls, and loop-end restart waiting. The final countdown number remains until the exact playback onset, when a compact Go cue and first-event pulse identify the intended first-note moment. Any computer key or MIDI note restarts a loop through a fresh countdown; the restart input is not scored, the waiting prompt leaves score/results fully visible without fading, and Stop remains available to end the loop instead.
- The Play-mode selector includes Pause at each note. A correct note-on for the next gate within the configured early hit tolerance now earns both its green result and gate credit, preventing an already accepted single note from forcing a second press at onset. Incomplete gates freeze at their written onset; ordinary chord partials carry forward only while held concurrently, while marked arpeggios retain ordered release-friendly progress. Rapid multi-note bursts are drained event-by-event even when React batches their state updates. The next unopened gate remains authoritative for keyboard expectations and audio lookahead, so early completion opens scheduled score audio without sounding it before the written onset. Recorded markers follow the ordinary deferred/live visibility preference.
- Untimed scored Practice reuses the same gate, chord, arpeggio, selection, hand-filter, Stop/Reset, and performance-result machinery, but moves directly to the next gate as soon as the current expectation is satisfied. It bypasses countdown, the Go cue, and metronome playback; has no inter-note tempo deadline; records wrong pitches in red and accepted pitches in green at each written location; and follows the same deferred/live marker preference as Play. Notes that completed a gate remain neutral-carried until physically released, preventing them from flashing as mistakes against the next expectation in Practice or Pause at each note.
- MusicXML arpeggios are treated as ordered gestures rather than simultaneous chords in both untimed practice and Pause-at-each-note gates. Direction defaults upward or follows an explicit downward mark; successive notes may be released but must arrive in order within a 750ms gap. The waiting prompt names only the next rolled pitch, the sidebar labels the arpeggio and direction, and synthesized score playback spreads rolled notes by 70ms.
- Synthesia is independently visible even when piano keys are hidden. Its pointer-through lane panel rises from the keyboard or permanent toolbar strike edge, clamps resizing to 6px below the measured title header, and keeps persisted transparent/opaque, falling-note-label, and Slow/Normal/Fast options in its attached popover. Countdown time positions blocks precisely; visible keys fill with the matching hand colour at the strike point, note-gated playback freezes that state until the required input, and very short written notes retain the strike highlight briefly without changing transport timing.
- The selection-relative timeline remains full width below the permanent toolbar whenever a playable score is loaded, with hand-coloured event marks, elapsed/total time, a live playhead, and event-snapped seeking. Timed seeking creates a clean paused attempt at the destination; untimed seeking moves the learning cursor directly.
- The score auto-follows the displayed cursor by smoothly bringing each newly active notation system to the top of the viewport in timed playback and untimed practice.
- OSMD and every app-owned overlay anchor now refresh after observed score-width changes and window/screen resize events, covering live sidebar resizing, panel collapse, margin adjustment, viewport resize, and moving the browser between displays without reloading MusicXML.
- A single score click moves to the nearest hand-filtered playable event, while movement beyond a small threshold retains drag selection. Clicks outside an existing selection are ignored; timed clicks use the same clean paused seek as the timeline.
- MIDI note-on attempts are timestamped at the browser input boundary. Each written event/pitch accepts one deduplicated green in-tolerance result centered over the expected note. Wrong-pitch and mistimed attempts remain individual red markers at the played pitch and interpolated score-time position. Results can be shown live via a persisted option but default to appearing after Stop/completion; expected pitches without a valid green result receive a compact red X at natural completion.
- Natural completion in any Play mode now produces a first session-only performance score from assessable note slots, deduplicated hits, misses, and bad attempts. The fixed header shows the last score with a reduced-motion-aware count-up and an accessible hover/focus/click panel containing last, best, average, exercise context, note totals, error breakdowns, hit rate, and timed-Play timing error. Bests and aggregates are separated by score content, selection, hand filter, and Play mode; Stop, Pause, Reset, Clear, and seeking never finalize an attempt, while every completed loop pass does.
- The fixed header shows active-exercise remaining time over ideal time beside Open Score. Ideal duration follows the tempo map, tempo percentage, hand filter, selection, and actual starting event. The active clock excludes count-in, manual pause, and loop-restart waiting, includes required-note waits, and freezes after Stop or completion until Reset or a new attempt. Tempo Play scores note accuracy alone. Pause at each note and Practice blend 80% accuracy with 20% pace, awarding full pace through a human grace allowance of the larger of 500ms or 5% of ideal duration; negative time stays neutral during grace and turns red only when pace begins falling.
- A simulation button can advance events without hardware.
- Debug panel shows loaded file, selected MIDI device, last MIDI message, held notes, ignored carried notes, expected event, event index, selected-hand playability, next playable index, parser warnings, import diagnostics, comparison results, and practice-attempt diagnostics.

## Recently Completed
- Practice Readiness adds a skippable Keyboard check to Learning Home. It reuses the live MIDI connection and guides the learner through lowest comfortable key, physical middle C, and highest comfortable key. Only fresh notes played after opening/resetting the check are captured; it validates middle C as C4, confirms low–middle–high ordering, shows held-note state, and gives concrete octave/transpose troubleshooting. An optional sustain check separately confirms pedal-down and pedal-up CC64 messages plus current state while explaining that pedal data is not yet part of performance scoring. Learning Home also offers a session-only route finder: experience plus current goal produces one explainable launch into an existing activity without saving a profile or locking navigation. Practice Readiness remains optional and never blocks direct practice. See `docs/PRACTICE_READINESS_PLAN.md`.
- Guided Practice now shows the latest assessed result inside every completed playing step and focused detour: a compact prominent percentage plus Passed at 90% or above, or Try again below it. Listen remains intentionally unscored, and starting a fresh plan or replacing the score clears these session-only step results.
- Added an optional global Play setting that waits for a MIDI piano strike before beginning the normal count-in in every playing mode. The readiness strike is consumed rather than assessed, Stop cancels the armed state, and loop restarts reuse the same readiness path so enabling the option never creates two consecutive waits.
- Expanded Learning practice generation from C/D major to all 68 chord and 68 scale catalog entries using shared MusicXML templates and catalogue-wide invariant tests.
- Kept Synthesia populated while idle and anchored its roll to the currently selected score event, so score clicks and timeline seeks preview that point as the next playable onset before playback begins.
- Corrected Synthesia note lengths for mixed-duration onsets by preserving each MusicXML note's written duration and carrying its tempo-aware end time into the playback plan; short repeated pitches no longer stretch across sustained notes from another voice.
- Made both synchronized Loop controls live throughout playback. The current setting governs completion, and switching Loop off at the restart prompt finishes immediately while retaining results.
- Added the first Learning surface as a data-driven four-column chord and scale reference chart, then proved its lesson seam with clickable C-major and D-major chord and two-staff scale exercises generated as ordinary in-memory MusicXML.
- Added a session-only See Note inspection tool to both synchronized toolbars. In idle or paused states it resolves individual OSMD graphical noteheads to their written MusicXML pitch spelling, shows a clamped note-anchored tooltip, and overlays a configurable striped highlight on the matching piano key. Hidden or range-limited pianos expand temporarily without changing saved presentation settings.
- Added meter-aware metronome playback, musical count-in bars, proportional score-tempo adjustment, independent click volume, and tempo-specific performance history.
- Replaced the legacy sidebar Simulate/Release pair with one silent contextual simulation action on the expected-event card. It advances the current hand-filtered note, chord, or arpeggiated event and releases its on-screen held-key pulse automatically after 180ms.
- Changed on-screen piano audition from fixed one-shot clicks to held pointer gestures: a key sustains while pressed, dragging creates a glissando, and leaving then returning to a key starts and sustains it again.
- Implemented the first browser-only PoC architecture.
- Added deterministic unit tests for core MIDI, note, parser, matcher, progress logic, and renderer overlay behavior.
- Added architecture and decision documentation.
- Added `Samples/` to `.gitignore`; local sample files remain available for manual testing but are not committed.
- Verified through a temporary local-only test that `Samples/Mad_world_Piano.mxl` decompresses and produces playable score events.
- Fixed a renderer lifecycle bug where MIDI/debug rerenders could reload OpenSheetMusicDisplay and cause severe memory growth per keypress.
- Reworked score selection so dragging and resize handles use freeform pointer previews and resolve to score events only on release.
- Added OSMD graphical-event anchoring, narrower current-event markers, responsive OSMD-managed system wrapping, and MusicXML print-break import diagnostics for staff/layout troubleshooting.
- Fixed rest-over-pitched-note parsing so rests in another voice at the same timestamp do not hide playable notes.
- Added green correct-note feedback, red wrong-note feedback, independent note-name toggles, and carry-over suppression for notes still held after a correct advancement.
- Added the read-only bottom piano panel and reusable piano settings/geometry/state module.
- Added the first timed playback and performance-marking foundation without sound generation.
- Added MIDI-gated timed playback as the transport foundation used by the Synthesia-style mode.
- Added the initial transparent Synthesia falling-note view using the shared piano and timed-transport geometry.

## Work In Progress
- Basic Sight-Reading Mode now generates fresh deterministic exercises from a three-level setup with RH, LH, or both hands, selectable 4/8/12-measure length, a written tempo of 60/80/100/120 BPM, and unlimited/15/30/60-second preparation. A timed preparation automatically enters the same clean bar-one attempt path as the manual action and bypasses the global wait-for-MIDI gate while retaining the musical count-in. Beginner retains steady quarter notes; Developing and Intermediate introduce mixed quarter/eighth patterns and wider ranges. The session provides one continuous attempt with score and piano correctness feedback suppressed. Its dedicated result separates note accuracy from the share of score moments attempted (“Kept moving”), combines them 75/25 into a reading score, and does not write into ordinary rehearsed-piece performance history. Completed excerpts lock the ordinary transport; Review excerpt deliberately replays from bar one without replacing the first-look result. Existing modifier-qualified piano shortcuts are contextual here too: Primary starts or advances to a new excerpt, Repeat starts review, Previous returns to Learning outside the active run, and Stop stops playback/review. Generated tempo directions use complete MusicXML metronome markup so OSMD renders every note measure and stays aligned with playback/scoring; wrong-note markers are constrained to the assessed staff. Physical-MIDI difficulty calibration remains next. See `docs/SIGHT_READING_PLAN.md`.
- Guided Practice Milestone 4 diagnosis is implemented in code. Assessed lesson passes retain session-only evidence by section boundary revision, measure, and hand. Once a lesson completes its two target-tempo runs, a Practice insight card identifies the measure with the strongest concrete note-error evidence, names a hand only when clearly dominant, and provides expandable supporting counts. Its direct “Start focused practice” action opens a temporary one-measure, hand-aware Pause-at-each-note detour; focused attempts update evidence, while return restores the exact lesson context without rewriting its boundaries or completion. Done Listening now also stops any active audition before completing the step.
- Piano Shortcuts Milestone 2.5 received positive initial real-keyboard validation. Settings → Controls provides opt-in, versioned local modifier/action bindings with learn-by-playing capture, conflict reporting, optional guide hints, and reset defaults. The default modifier is A1 so the modifier and action notes fit under the left hand; version-1 defaults migrate from A0. During Guided Practice, modifier-qualified Primary/Previous/Repeat/Loop/Stop commands are recognised once per fresh press and consumed before held-note feedback or assessment.
- Guided Practice Milestone 3 is implemented in code and awaiting physical-MIDI validation. Every lesson gains a final Tempo stage using continuous Play rather than note-paused practice. It begins at 60%, requires a score of at least 90 before offering an explicit 10-point increase, targets written tempo at 100%, and requires two qualifying target runs before marking the lesson comfortable. The guide shows current/target tempo, target-run count, score-dependent repeat/increase decisions, and restores the pre-guide tempo on exit.
- User testing reports that the multi-lesson Tempo flow works well. Settings now has a Learning category with persisted Tempo build-up presets: Gentle (60–100), Steady (80–100), and At tempo (100-only), all retaining the same qualification rules. New plans snapshot the chosen starting tempo so changing the preference cannot rewrite work already underway; boundary resets continue using that plan's original preset.
- Guided Piece Practice Milestone 1 is user-confirmed, and the Milestone 2 session shell is implemented. Done Planning preserves the selected lesson and opens a guide with lesson selection, a hand-aware step rail, focus card, skip/repeat/leave actions, and section progression. RH-only or LH-only lessons omit the absent hand and unnecessary Both stage; two-hand lessons retain Listen/RH/LH/Both. Completed/current step circles support backward navigation without changing the plan. Listen-through uses toggleable direct non-assessed score audio. Hand stages reuse Pause-at-each-note playback, keep the guide visible, record every completed pass as a section attempt, and restore the user's previous hand and play mode when leaving. A guide-local Loop option is synchronized with the existing transport loop: each pass is recognised, then the learner can repeat immediately or explicitly continue. Broader imported-score and real-MIDI validation remains pending; `docs/GUIDED_PRACTICE_IMPLEMENTATION_PLAN.md` is the resumable checklist.
- All chord and scale catalog entries generate initial practice scores. Musical review of rare spellings/fingerings, richer lesson progression, and retaining lesson/navigation context remain future work.
- Current baseline is usable for `Samples/Mad_world_Piano.mxl` per user feedback. Wrong/correct note reporting is improved but still needs more real-keyboard refinement.
- A Windows 10/11 portable-test packaging path is available through `npm run package:portable`. It creates an ignored ZIP containing only the production app, a double-click launcher, a loopback-only PowerShell static server, tester instructions, and build identity; the destination computer needs Edge or Chrome but no Node.js or developer installation. The fixed `http://127.0.0.1:4173` origin preserves Web MIDI eligibility and local preferences between launches.
- The future online product direction is now documented in `docs/PLATFORM_ROADMAP.md`: retain browser-local real-time music processing, add Vercel hosting plus EU-region Supabase accounts/private storage/Postgres in phases, introduce platform-curated courses, and add Stripe-derived entitlements only after the learning and persistence foundations are proven. No backend, account, upload, or billing implementation exists yet; the current runtime remains browser-only.
- The first Pause-at-each-note and Synthesia implementation is complete and considered a strong initial form. Further visual/timing refinements are expected after broader hands-on testing rather than before this checkpoint is committed.
- The first synthesized score-audio implementation is complete in code. Sound quality, onset feel, chord balance, Pause-gate behavior, mute/volume ergonomics, and stop/loop cleanup still need hands-on Chromium speaker and MIDI testing.
- Manual pause/resume, timeline seeking, Reset, global Space handling, and system auto-follow are implemented and covered by deterministic tests where practical, but still need hands-on Chromium validation for scroll feel, focus behavior, layout, and audible resume cleanup.
- `Samples/Final_Fantast_IV_The_Prelude_-_Piano_Solo.mxl` appears to encode its opening pitched notes on staff 1 only, so left-hand practice has little/no opening material for that file. Other tested scores appear to split hands normally.

## Known Issues Or Blockers
- Written repeat expansion is deferred; the parser follows printed measure order and reports repeat warnings.
- Score overlays prefer OSMD graphical measure/timestamp mapping instead of cursor-step sampling, but exact notehead-level placement is still deferred.
- Correct/wrong score feedback uses rendered staff-line geometry plus MusicXML/key-signature-aware diatonic steps, but remains an app-owned overlay rather than exact rendered notehead geometry.
- Tied stop-only notes are skipped as re-strikes, but tie durations are not merged into extended event durations.
- Real MIDI hardware has been partially validated by the user: device detection, keypress display, Mad World score rendering, score selection, and basic progression are working well. Feedback behavior still needs follow-up polish.
- No `.mid` playback/comparison path is implemented; the `.mxl` score remains the source of truth.
- MIDI-input sound generation, performance export, and note-off/held-duration assessment remain deferred. The first falling-note lanes are visual and use written onset/duration only. The current score and pointer-audition voices are synthesized; a sampled piano remains a later audio-quality upgrade.
- Timed performance marking currently scores note-on pitch/onset only; velocity, held duration, note-off timing, sustain quality, and export remain deferred. Score audio uses fixed synthesized dynamics and does not yet interpret MusicXML dynamics, sustain, or articulation.
- The two `ScoreRenderer` geometry failures were resolved by anchoring completed feedback to the written-note position and mirroring pointer interaction mode in a ref so batched pointer events retain the full drag preview. Automated coverage now also includes three-mode scoring, natural-completion snapshots, session aggregates, and the accessible header score summary; use the current validation output rather than this handover paragraph as the authoritative test count.
- The current managed Codex environment blocks Vite/Vitest's normal config bundling and Vitest fork workers with `spawn EPERM`; native config loading plus `--pool=vmThreads --maxWorkers=1` runs validation successfully. No in-app browser target was attached during the latest refinement, so Synthesia and score-audio behavior still need local Chromium and real-speaker verification.
- The portable package has passed build, archive-content, loopback HTTP, MIME/HEAD, duplicate-launch, missing-file, and path-traversal checks. It still needs extraction and hands-on validation on the destination Windows computer with its installed Chromium browser, audio output, MIDI drivers, and physical keyboard.

## Important Assumptions
- Desktop Chromium is the initial supported browser target.
- Localhost/127.0.0.1 is sufficient for Web MIDI development because it is treated as a secure context.
- The user will handle remote pushes.
- `Samples/` may contain copyrighted or third-party music and should remain local-only unless explicitly approved for commit.

- See Note has automated coverage for written accidentals, colour validation, and temporary piano reveal/range expansion, but still needs manual desktop-Chromium hover validation on single notes, chords, both staves, edge-positioned tooltips, hidden piano, and out-of-range pitches.

## Recommended Next Steps
- Validate Milestone 4 diagnosis and its “Start focused practice” detour with the physical MIDI keyboard: deliberately miss notes in different measures/hands, verify the primary insight, complete and repeat a focused pass, then confirm Return restores the completed lesson and Next lesson remains unaffected.
- Validate Guided Practice Milestone 3 with the user's real MIDI keyboard: complete a Tempo-stage run below and above the 90-point threshold, confirm explicit 10-point progression, complete two qualifying 100% runs, and exercise repeat/loop/piano-shortcut behavior plus tempo restoration on exit.
- Extract `out/DigiPiano-0.1.0-win-portable.zip` on the destination Windows computer, start it through `Start Piano.bat`, and validate Edge/Chrome launch, MIDI permission/device access, score import, audio, persisted settings, and shutdown/relaunch behavior. Report issues with the included `VERSION.txt` details.
- Treat `docs/PLATFORM_ROADMAP.md` as the source for future account, private score-bank, saved-selection/bookmark, progress, course, billing, and public-content work. When platform implementation begins, start with its Phase 1 domain boundaries rather than adding a backend SDK directly to playback or renderer code.
- Validate the new Play-mode modal from both notation and bottom toolbars: radio selection, persistence, keyboard/focus behavior, reset-on-change during a run, and removal of the former standalone Pause-at-each-note button. Exercise untimed scored Practice with chords, arpeggios, wrong notes, hand filters, selections, Stop/Reset, loops, and marker visibility.
- Validate the current build in desktop Chromium: confirm compact Open Score/Settings header buttons, full RH/LH fills in untimed practice and Play, complete dim gate previews appearing one local quarter-note beat early, full-strength gate arrival, duration-aware correct-to-neutral held notes, marker-preserving Stop-to-start, and result-clearing Reset.
- Start by validating the full-height Practice/Debug workspace in Chromium with Synthesia collapsed and expanded: confirm layering above the right side of the piano/timeline, scrolling, resizing, tab keyboard navigation, collapse/restore, mandatory hide-on-play, manual mid-run restoration, and optional fullscreen entry/exit.
- Start with real-browser and MIDI-keyboard testing of Pause-at-each-note and Synthesia across several scores, ranges, tempos, hand filters, piano widths, transparent/opaque modes, and resized roll heights.
- Test score audio through real speakers across countdowns, full scores and selections, RH/LH filters, Pause gates, loops, Stop, mute, and live volume changes; note onset drift, stuck notes, clipping, or harsh chord balance before tuning the synth.
- Refine Synthesia only from observed use: block speed/readability, label density, staff colours, auto-follow behavior, strike/key feedback, and toolbar ergonomics are the likely adjustment points.
- Continue with wrong/correct note feedback polish: validate green markers, red markers, label toggles, and held-note carry-over suppression on a real keyboard.
- Validate the 450ms completed-correct linger/fade timing with a real keyboard and adjust only if it feels too fast or distracting.
- Confirm once/loop practice uses resized ranges correctly across several scores.
- Use import diagnostics when a score appears to assign notes to the wrong hand; verify `firstPitchedMeasureByStaff`, `firstMeasures`, and `firstParsedEvents` before changing parser behavior.
- Decide whether the OSMD graphical-event overlay strategy is accurate enough for the next milestone or whether deeper notehead-level renderer integration is required.
- Decide how to handle repeat expansion before moving beyond the proof of concept.
- Consider MIDI file use only if it adds concrete value for playback-order validation or reference playback.

## Relevant Files
- `src/App.tsx`
- `src/components/ScoreRenderer.tsx`
- `src/components/PianoPanel.tsx`
- `src/piano/synthesia.ts`
- `src/playback/usePlaybackSession.ts`
- `src/music/musicXmlLoader.ts`
- `src/music/musicXmlParser.ts`
- `src/midi/messages.ts`
- `src/midi/heldNotes.ts`
- `src/hooks/useMidiInput.ts`
- `src/learning/matcher.ts`
- `docs/ARCHITECTURE.md`
- `docs/DECISIONS.md`
- `docs/ROADMAP.md`
